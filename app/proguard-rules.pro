# Add project-specific ProGuard rules here.
# Room, Hilt, Moshi, Media3, and Rive all ship consumer-rules.pro in their AARs,
# so no manual keep rules should be needed for them by default — only add rules
# here if you hit a specific ClassNotFoundException/NoSuchMethodError in a
# minified release build.
